// import React,{Component} from 'react';

// class WriteReview extends Component{
//     render(){

//       let listing = this.props.currentListing;
//        let review = e.target.querySelector('review').value;
//        let name = e.target.querySelector('name').value;

        
//         return(
//             <form action="" onSubmit ={()=>{this.props.WriteReview(listing,review,name)}}>
//                 <input type="text" id="review"/>
//                 <input type="text" id="name"/>
//                 <button>Submit</button>
//             </form>
//         )
//     }
// }

// export default WriteReview;

// onsubmit it should take the value inside input,and send it to the app component where a function will take the listing
// ,find the reviews array, and push the new review into the array and change the state of the listing.